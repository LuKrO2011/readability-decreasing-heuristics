package de.uni_passau.fim.se2.rdh.config;

/**
 * The processing scope of the tool can be either a file or a directory.
 */
public enum Scope {
    /**
     * If the processing scope is file.
     */
    FILE,
    /**
     * If the processing scope is directory.
     */
    DIRECTORY
}
